// Palette
import palette from '../palette';

const style: any = {
  root: {
    borderRadius: '3px',
    overflow: 'hidden'
  },
  colorPrimary: {
    backgroundColor: palette.common.neutral
  }
};

export default style;
