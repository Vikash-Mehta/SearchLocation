import IConfig from 'configInterface';

const config: IConfig = {
  api: 'http://localhost:5000',
  frontendURL: 'http://localhost:9000/',
  preAuthKey: 'remember_me_token',
};

export default config;
